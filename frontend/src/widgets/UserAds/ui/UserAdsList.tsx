'use client'

import { usePathname } from 'next/navigation'

import { Ad } from '@/src/entities'
import { ErrorMessage, Skeleton, useAds, useUserStore } from '@/src/shared'

interface Props {
	userId?: string
}

export const UserAdsList = ({ userId }: Props) => {
	return (
		<div className=''>
		
		</div>
	)
}
